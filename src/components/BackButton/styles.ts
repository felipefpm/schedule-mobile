import styled from 'styled-components/native';
import { TouchableOpacity } from 'react-native';

export const StyledTouchableOpacityBackButton = styled(TouchableOpacity)`
  background-color: #141313;
  margin-top: 16%;
`;